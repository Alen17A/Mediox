import 'package:flutter/material.dart';

class AddPlaylists extends StatelessWidget {
  const AddPlaylists({super.key});

  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: () {},
      child: const Icon(Icons.add),
    );
  }
}
